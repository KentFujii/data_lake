apiVersion: v1
kind: ConfigMap
metadata:
  name: data-lake-cm
  namespace: data-lake-ns
data:
  openapi.yml: |
    {{ openapi | indent(width=4, indentfirst=False) }}
  airflow.cfg: |
    {{ airflow | indent(width=4, indentfirst=False) }}
  unittests.cfg: |
    {{ unittests | indent(width=4, indentfirst=False) }}
